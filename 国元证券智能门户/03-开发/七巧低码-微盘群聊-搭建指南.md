# 七巧低码搭建指南 — 微盘共享空间管理 + 部门群聊管理

> 📅 2026-07-29 | API域名：qw.oa.gyzq.com | corpid：ww3a805f9cde3da4c2

---

## 准备工作

### 需要确认的信息

| # | 事项 | 状态 |
|:--:|------|:---:|
| 1 | 微盘应用secret | ✅ `IcJJQnQJKXGPIdtu_-HSzTZpawKtliVmrZs7iB1SlCo` |
| 2 | 群聊应用secret | ⚠️ **待补充** |
| 3 | 程文斐企微userid | ⚠️ **待确认**（脚本通知用）|
| 4 | 七巧低代码平台地址 | ⚠️ **待确认** |
| 5 | 企微管理后台 → 应用管理 → 微盘/群聊App → 已配置"可调用接口的应用"列表 | ⚠️ **需确认** |

---

## 一、应用1：微盘共享空间管理

### 1.1 创建应用

- 应用名称：`微盘共享空间管理`
- 应用图标：建议使用微盘相关图标

### 1.2 流程表单1：共享空间新建

#### 表单字段

| 序号 | 字段名 | 控件类型 | 必填 | 说明 |
|:--:|--------|---------|:--:|------|
| 1 | 空间名称 | 单行文本 | ✅ | 如"信息技术部-共享空间" |
| 2 | 申请部门 | 部门选择器 | ✅ | |
| 3 | 空间管理员（可选） | 成员选择器（多选，最多2人） | ❌ | 除申请人外额外指定 |
| 4 | 申请容量（GB） | 数字 | ❌ | 仅记录 |
| 5 | 申请事由 | 多行文本 | ✅ | |
| 6 | 空间ID（spaceid） | 单行文本（只读） | - | 脚本回填 |
| 7 | 创建结果 | 单行文本（只读） | - | 脚本回填 |
| 8 | 创建时间 | 日期时间（只读） | - | 脚本回填 |

#### 审批流程

```
申请人提交 → IT管理员审批（程文斐）
```

#### 脚本配置

- **触发时机**：执行后（审批通过后）
- **脚本来源**：`七巧低码-微盘群聊-脚本集.md` → 二 → 2.1
- ⚠️ 需同时将公共函数 `getWeDriveToken()` 复制到脚本开头

---

### 1.3 流程表单2：共享空间扩容

#### 表单字段

| 序号 | 字段名 | 控件类型 | 必填 | 说明 |
|:--:|--------|---------|:--:|------|
| 1 | 选择空间 | 下拉选择 | ✅ | 列出已有空间 |
| 2 | 当前容量 | 只读显示 | - | 自动显示 |
| 3 | 扩容至（GB） | 数字 | ✅ | |
| 4 | 扩容原因 | 多行文本 | ✅ | |
| 5 | 处理状态 | 单选（只读） | - | 待处理/已处理，脚本写"待处理" |
| 6 | 处理结果 | 单行文本（只读） | - | |

#### 审批流程

```
申请人提交 → IT管理员审批（程文斐）
```

#### 脚本配置

- **触发时机**：执行后
- **脚本来源**：`七巧低码-微盘群聊-脚本集.md` → 二 → 2.2

---

### 1.4 流程表单3：空间管理员变更

#### 表单字段

| 序号 | 字段名 | 控件类型 | 必填 | 说明 |
|:--:|--------|---------|:--:|------|
| 1 | 选择空间 | 下拉选择 | ✅ | |
| 2 | 原空间管理员 | 只读显示 | - | |
| 3 | 新管理员 | 成员选择器 | ✅ | |
| 4 | 变更原因 | 多行文本 | ✅ | |
| 5 | 新管理员添加结果 | 单行文本（只读） | - | 脚本回填 |
| 6 | 通知结果 | 单行文本（只读） | - | 脚本回填 |

#### 审批流程

```
申请人提交 → IT管理员审批（程文斐）
```

#### 脚本配置

- **触发时机**：执行后
- **脚本来源**：`七巧低码-微盘群聊-脚本集.md` → 二 → 2.3
- ⚠️ 需同时复制 `getWeDriveToken()` 函数

---

## 二、应用2：部门群聊管理

### 2.1 创建应用

- 应用名称：`部门群聊管理`

### 2.2 流程表单1：创建部门群

#### 表单字段

| 序号 | 字段名 | 控件类型 | 必填 | 说明 |
|:--:|--------|---------|:--:|------|
| 1 | 群名称 | 单行文本 | ✅ | 如"信息技术部-部门群" |
| 2 | 所属部门 | 部门选择器 | ✅ | 自动拉该部门全员进群 |
| 3 | 群主 | 成员选择器 | ✅ | 默认填申请人 |
| 4 | 额外成员 | 成员选择器（多选） | ❌ | 部门外的额外成员 |
| 5 | 申请事由 | 多行文本 | ✅ | |
| 6 | 群ID（chatid） | 单行文本（只读） | - | 脚本回填 |
| 7 | 创建结果 | 单行文本（只读） | - | 脚本回填 |
| 8 | 群成员数 | 数字（只读） | - | 脚本回填 |

#### 审批流程

```
申请人提交 → IT管理员审批（程文斐）
```

#### 脚本配置

- **触发时机**：执行后
- **脚本来源**：`七巧低码-微盘群聊-脚本集.md` → 三 → 3.1
- ⚠️ 需同时复制 `getAppChatToken()` 函数，并**替换secret占位符**

---

### 2.3 流程表单2：群成员管理

#### 表单字段

| 序号 | 字段名 | 控件类型 | 必填 | 说明 |
|:--:|--------|---------|:--:|------|
| 1 | 选择群 | 下拉选择 | ✅ | 列出该应用创建的群 |
| 2 | 操作类型 | 单选 | ✅ | 添加成员 / 移除成员 |
| 3 | 选择成员 | 成员选择器（多选） | ✅ | |
| 4 | 操作原因 | 多行文本 | ✅ | |

#### 审批流程：同上

#### 脚本配置

- **触发时机**：执行后
- **脚本来源**：`七巧低码-微盘群聊-脚本集.md` → 三 → 3.2

---

### 2.4 流程表单3：群信息变更

#### 表单字段

| 序号 | 字段名 | 控件类型 | 必填 | 说明 |
|:--:|--------|---------|:--:|------|
| 1 | 选择群 | 下拉选择 | ✅ | |
| 2 | 操作类型 | 单选 | ✅ | 改群名 / 改群主 / 解散群 |
| 3 | 新群名 | 单行文本 | 仅改群名 | |
| 4 | 新群主 | 成员选择器 | 仅改群主 | |
| 5 | 变更原因 | 多行文本 | ✅ | |

#### 审批流程：同上

#### 脚本配置

- **触发时机**：执行后
- **脚本来源**：`七巧低码-微盘群聊-脚本集.md` → 三 → 3.3

---

## 三、部署检查清单

### 3.1 企微后台配置

- [ ] 微盘应用 → 已加入"可调用接口的应用"列表
- [ ] 群聊应用 → 已加入"可调用接口的应用"列表
- [ ] 应用secret已确认可获取access_token

### 3.2 七巧应用配置

- [ ] 两个应用已创建
- [ ] 6个流程表单已搭建
- [ ] 审批流程已配置（审批人：程文斐）
- [ ] 脚本已粘贴到"执行后"位置
- [ ] 群聊脚本secret占位符已替换
- [ ] 公共函数已复制到对应脚本前

### 3.3 权限配置

- [ ] 应用发布范围设置正确
- [ ] 部门负责人可见对应表单
- [ ] IT审批人可审批

### 3.4 测试验证

- [ ] 微盘：提交一个空间新建申请 → 审批通过 → 检查企微后台是否有新空间
- [ ] 微盘：提交扩容申请 → 审批通过 → 检查程文斐是否收到通知
- [ ] 微盘：提交管理员变更 → 审批通过 → 检查权限是否变更
- [ ] 群聊：提交创建群申请 → 审批通过 → 检查企微是否有新群
- [ ] 群聊：提交成员管理 → 审批通过 → 检查群成员是否变更
- [ ] 群聊：提交群信息变更 → 审批通过 → 检查是否生效

---

## 四、待确认信息汇总

| # | 信息 | 影响范围 |
|:--:|------|---------|
| 1 | 群聊应用secret | 群聊全部3个场景使用 |
| 2 | 程文斐企微userid | 扩容通知/管理员变更通知 |
| 3 | 七巧平台地址 | 搭建需要在平台上操作 |
| 4 | 是否需要限制每个部门可创建的微盘空间数量 | 空间新建场景 |
| 5 | 扩容是否接受"审批后通知管理员手动操作"模式 | 微盘扩容场景 |
